# 构建中间产物（体积大、可由 build.py 重新生成）。
# 注意：mapping.json 是前端运行所必需的，必须提交，不要忽略。
all_basic_hanzi.json

# 映射报告（由 build.py 生成，仅供本地查看）。
report.md

# 日志
logs/

# Python
__pycache__/
*.pyc

# 系统 / 编辑器
.DS_Store
.vscode/
.idea/
